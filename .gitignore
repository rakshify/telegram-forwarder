__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# secrets
.env

# local session storage (also matches the docker-compose volume mount)
data/
sessions/
*.session
*.session-journal

# sample docker compose
docker-compose.sample.yml

# real configs may contain access_hashes — keep examples, ignore everything else
configs/*.json
!configs/*.example.json

# editor / OS
.idea/
.vscode/
.DS_Store
